function plot_heatmap(all_data)
    % Create a ranked heatmap of firing rate changes (percent change and z-score).

    % (This function generates a heatmap with modulated units ranked from 
    % most positively to most negatively modulated.)
end
